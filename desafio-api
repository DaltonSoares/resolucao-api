#Abaixo os desafios que preciso resolver.

Fique à vontade para utilizar qualquer linguagem que achar útil. Caso não consiga concluir esta atividade por favor entregue o que for feito até a data solicitada.
Citar as tecnologias e IDE’s, linguagem e ferramentas utilizadas para confecção da análise.
Citar as referências utilizadas.
1 - Gerar uma visão(dashboard/Indicador) das top10 APIS que tiveram maior tempo de resposta.
2 - Gerar uma visão(dashboard/Indicador) das top10 APIS que tiveram mais erros.
3 - Gerar uma visão(dashboard/Indicador) por hora de uso das APIs
4 - Qual API trafegou mais dados no período do log enviado e como você chegou a essa conclusão.
5 - Elaborar a documentação de maneira simples e apresentar no dia da entrevista.



#RESOLUÇÃO
1 - sort -k3 log_api.log >> sort_log_api.log #executado comando para organizar pela terceira coluna, ou seja, coluna das APIs e gerar um novo arquivo chamado sort_log_api.log.

2 - #comando para saber quantas apis tem. 
	  #identifiquei que tem 7 APIs.
	
    $ cat sort_log_api.log | awk '{print $3}' | uniq
    "/api/cliente/token"
    "/api/perfil/cliente"
    "/api/produto/baixa"
    "/api/produto/cliente"
    "/api/produto/leilao"
    "/api/produto/pagamento"
    "/api/produto/vendas"

OBS: Informado ao Bebert que o case pedia para verificar as TOP10 APIs porém tem apenas 7 e ele disse que está correto, isso já era um dos desafios do teste.

3 - Analisando bem a estrutura do arquivo vou tentar inserir os dados em um banco mysql. Acredito que será mais fácil para filtrar os dados.
    - Instalei o MySQL no WINDOWS.
    
    - Criando tabela API para inserir os dados.

	CREATE TABLE API (
	    ID  INT PRIMARY KEY AUTO_INCREMENT,
	    DATA_OPERACAO TIMESTAMP,
	    METODO VARCHAR(255),
	    API VARCHAR(255),
	    VERSAO_HTTP VARCHAR(255),
	    COD_RESPOSTA INT,
	    BYTES INT,
	    TEMPO_RESPOSTA INT    
	);

	- No próprio arquivo de log enviado alterei o formato da data para AAAA-MM-DD para poder fazer o insert no banco de dados.
	- Antes de fazer o insert formatei no Excel somente para garantir que os dados para o insert estivessem corretos.
	


4 - Usando Grafana web - https://daltonsoares.grafana.net/?orgId=1

